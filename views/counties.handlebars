<div class="row mb-2">
  {{#each counties}}
  <div class="col-md-3">
    <div class="card flex-md-row mb-4 box-shadow h-md-250">
      <div class="card-body d-flex flex-column align-items-start">
        
        <h3 class="mb-0">
          <a class="text-dark" href="/hotels/county/{{_id}}">{{name}}</a>
        </h3>
        <div class="mb-1 text-muted"></div>
        <p class="card-text mb-auto"></p>        
        <a href="/hotels/county/{{_id}}">View Accommodations</a>        
      </div>
      {{#if photo}}
        <img class="card-img-right flex-auto d-none d-md-block" src="/images/counties/{{photo}}" data-src="/images/counties/{{photo}}" alt="{{name}}">
      {{/if}}
    </div>
  </div>
  {{/each}}

</div>



<div class="row mb-2">
  {{#each categories}}
  <div class="col-md-6">
    <div class="card flex-md-row mb-4 box-shadow h-md-250">
      <div class="card-body d-flex flex-column align-items-start">
        <strong class="d-inline-block mb-2 text-primary">{{name}}</strong>
        <h3 class="mb-0">
          <a class="text-dark" href="/hotels/category/{{_id}}">{{name}}</a>
        </h3>
        <div class="mb-1 text-muted"></div>
        <p class="card-text mb-auto"></p>        
        <a href="/hotels/category/{{_id}}">View Accommodations</a>        
      </div>
      {{#if photo}}
        <img class="card-img-right flex-auto d-none d-md-block" src="/images/categories/{{photo}}" data-src="/images/categories/{{photo}}" alt="{{name}}">
      {{/if}}
    </div>
  </div>
  {{/each}}

</div>


